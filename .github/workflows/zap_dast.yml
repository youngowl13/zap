name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: "https://youngowl13.github.io/zap-test/"
          name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: "https://youngowl13.github.io/zap-test/"
          cmd_options: "-a"

          
      - name: Save Report
        uses: actions/upload-artifact@v2
        with:
          name: ZAP Report
          path: report.html

      - name: Save Report
        uses: actions/upload-artifact@v2
        with:
          name: ZAP Report
          path: report.html
